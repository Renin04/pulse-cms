## What & why


## Changes
-

## Checklist
- [ ] `npm run ci:local` passes (lint + typecheck + tests)
- [ ] Tests added/updated for new behavior
- [ ] Engine packages stay framework-agnostic (no React/Next imports in `packages/core|blocks|editor|renderer`)
